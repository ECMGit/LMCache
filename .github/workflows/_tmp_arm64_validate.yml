name: TEMP arm64 validate (delete before PR)

# Throwaway: triggers the reusable aarch64 wheel build on push to this branch,
# so the manylinux_2_28_aarch64 cibuildwheel path can be validated in a fork
# without changing the default branch. DELETE this file before opening the PR.

on:
  push:
    branches:
      - tmp/arm64-ci-validate

permissions:
  contents: read

jobs:
  arm64:
    uses: ./.github/workflows/build_main_artifacts_arm64.yml
